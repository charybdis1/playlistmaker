<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:paddingHorizontal="@dimen/track_horizontal"
    android:paddingVertical="@dimen/track_vertical">

    <ImageView
        android:id="@+id/track_cover"
        android:layout_width="@dimen/track_cover_size"
        android:layout_height="@dimen/track_cover_size"
        android:layout_marginEnd="@dimen/track_vertical"
        android:scaleType="centerCrop"
        tools:src="@drawable/ic_placeholder" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            style="@style/track_text"
            android:id="@+id/track_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/track_name"
            android:textSize="16sp"
            tools:text="@tools:sample/lorem/random" />

        <TextView
            style="@style/track_text"
            android:id="@+id/track_info"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/color_track_info"
            android:textSize="11sp"
            tools:text="@tools:sample/lorem/random" />
    </LinearLayout>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/track_horizontal"
        android:src="@drawable/ic_arrow" />

</LinearLayout>